spring:
  cloud:
    config:
      server:
        git:
          uri: file:/config-repo
          default-label: main
          force-pull: true

logging:
  level:
    org.springframework.cloud.config: DEBUG
