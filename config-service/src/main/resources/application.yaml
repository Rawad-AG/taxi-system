spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_REPO_URI}
          search-paths: config-repo
          clone-on-start: true
          timeout: 10
