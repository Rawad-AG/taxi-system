services:
  # ============================================
  # INFRASTRUCTURE - PostgreSQL
  # ============================================
  db:
    build:
      context: ./db
      dockerfile: Dockerfile
    image: rawad0/db:latest
    container_name: db
    env_file: .env
    networks:
      - system-network
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ============================================
  # INFRASTRUCTURE - Cache
  # ============================================
  redis:
    image: redis:latest
    container_name: taxi-redis
    env_file: .env
    networks:
      - system-network
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ============================================
  # INFRASTRUCTURE - Message Broker (RabbitMQ)
  # ============================================
  rabbitmq:
    image: rabbitmq:latest
    container_name: taxi-rabbitmq
    env_file: .env
    networks:
      - system-network
    ports:
      - "5672:5672"
      - "15672:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "check_running"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ============================================
  # INFRASTRUCTURE - Monitoring
  # ============================================
  prometheus:
    build:
      context: ./monitoring-service/prometheus
      dockerfile: Dockerfile
    image: rawad0/monitoring-prometheus:latest
    container_name: taxi-prometheus
    env_file: .env
    networks:
      - system-network
    ports:
      - "9090:9090"
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"

  grafana:
    image: grafana/grafana:latest
    container_name: taxi-grafana
    env_file: .env
    networks:
      - system-network
    ports:
      - "3000:3000"
    depends_on:
      - prometheus

  # ============================================
  # SPRING CLOUD - Service Registry
  # ============================================
  discovery-service:
    build:
      context: ./discovery-service
      dockerfile: Dockerfile
    image: rawad0/taxi-discovery-service:latest
    container_name: taxi-discovery-service
    env_file: .env
    environment:
      - EUREKA_CLIENT_REGISTER_WITH_EUREKA=false
      - EUREKA_CLIENT_FETCH_REGISTRY=false
    networks:
      - system-network
    ports:
      - "8761:8080"
    depends_on:
      config-service:
        condition: service_healthy
    healthcheck:
      test:
        ["CMD-SHELL", "curl -f http://localhost:8080/actuator/health || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 5
    develop:
      watch:
        - action: rebuild
          path: ./discovery-service/src

  # ============================================
  # SPRING CLOUD - Config Server
  # ============================================
  config-service:
    build:
      context: ./config-service
      dockerfile: Dockerfile
    image: rawad0/taxi-config-service:latest
    container_name: taxi-config-service
    environment:
      - SPRING_PROFILES_ACTIVE=docker
    networks:
      - system-network
    ports:
      - "8888:8080"
    healthcheck:
      test:
        ["CMD-SHELL", "curl -f http://localhost:8080/actuator/health || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 5
    develop:
      watch:
        - action: rebuild
          path: ./config-service/src

  # ============================================
  # MICROSERVICES
  # ============================================
  auth-service:
    build:
      context: ./auth-service
      dockerfile: Dockerfile
    image: rawad0/taxi-auth-service:latest
    container_name: taxi-auth-service
    networks:
      - system-network
    ports:
      - "8081:8080"
    env_file: .env
    environment:
      - POSTGRES_DB=auth_db
    depends_on:
      discovery-service:
        condition: service_healthy
      config-service:
        condition: service_healthy
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test:
        ["CMD-SHELL", "curl -f http://localhost:8080/actuator/health || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 5
    develop:
      watch:
        - action: rebuild
          path: ./auth-service/src

  core-service:
    build:
      context: ./core-service
      dockerfile: Dockerfile
    image: rawad0/taxi-core-service:latest
    container_name: taxi-core-service
    networks:
      - system-network
    ports:
      - "8082:8080"
    env_file: .env
    environment:
      - POSTGRES_DB=core_db
    depends_on:
      discovery-service:
        condition: service_healthy
      config-service:
        condition: service_healthy
      db:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    healthcheck:
      test:
        ["CMD-SHELL", "curl -f http://localhost:8080/actuator/health || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 5
    develop:
      watch:
        - action: rebuild
          path: ./core-service/src

  location-service:
    build:
      context: ./location-service
      dockerfile: Dockerfile
    image: rawad0/taxi-location-service:latest
    container_name: taxi-location-service
    networks:
      - system-network
    ports:
      - "8083:8080"
    env_file: .env
    environment:
      - POSTGRES_DB=location_db
    depends_on:
      discovery-service:
        condition: service_healthy
      config-service:
        condition: service_healthy
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    healthcheck:
      test:
        ["CMD-SHELL", "curl -f http://localhost:8080/actuator/health || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 5
    develop:
      watch:
        - action: rebuild
          path: ./location-service/src

  fazaa-service:
    build:
      context: ./fazaa-service
      dockerfile: Dockerfile
    image: rawad0/taxi-fazaa-service:latest
    container_name: taxi-fazaa-service
    networks:
      - system-network
    ports:
      - "8084:8080"
    env_file: .env
    environment:
      - POSTGRES_DB=fazaa_db
    depends_on:
      discovery-service:
        condition: service_healthy
      config-service:
        condition: service_healthy
      db:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    healthcheck:
      test:
        ["CMD-SHELL", "curl -f http://localhost:8080/actuator/health || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 5
    develop:
      watch:
        - action: rebuild
          path: ./fazaa-service/src

  payment-service:
    build:
      context: ./payment-service
      dockerfile: Dockerfile
    image: rawad0/taxi-payment-service:latest
    container_name: taxi-payment-service
    networks:
      - system-network
    ports:
      - "8085:8080"
    env_file: .env
    environment:
      - POSTGRES_DB=payment_db
    depends_on:
      discovery-service:
        condition: service_healthy
      config-service:
        condition: service_healthy
      db:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    healthcheck:
      test:
        ["CMD-SHELL", "curl -f http://localhost:8080/actuator/health || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 5
    develop:
      watch:
        - action: rebuild
          path: ./payment-service/src

  notification-service:
    build:
      context: ./notification-service
      dockerfile: Dockerfile
    image: rawad0/taxi-notification-service:latest
    container_name: taxi-notification-service
    networks:
      - system-network
    ports:
      - "8086:8080"
    env_file: .env
    environment:
      - POSTGRES_DB=notification_db
    depends_on:
      discovery-service:
        condition: service_healthy
      config-service:
        condition: service_healthy
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    healthcheck:
      test:
        ["CMD-SHELL", "curl -f http://localhost:8080/actuator/health || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 5
    develop:
      watch:
        - action: rebuild
          path: ./notification-service/src

  rating-service:
    build:
      context: ./rating-service
      dockerfile: Dockerfile
    image: rawad0/taxi-rating-service:latest
    container_name: taxi-rating-service
    networks:
      - system-network
    ports:
      - "8087:8080"
    env_file: .env
    environment:
      - POSTGRES_DB=rating_db
    depends_on:
      discovery-service:
        condition: service_healthy
      config-service:
        condition: service_healthy
      db:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    healthcheck:
      test:
        ["CMD-SHELL", "curl -f http://localhost:8080/actuator/health || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 5
    develop:
      watch:
        - action: rebuild
          path: ./rating-service/src

  audit-service:
    build:
      context: ./audit-service
      dockerfile: Dockerfile
    image: rawad0/taxi-audit-service:latest
    container_name: taxi-audit-service
    networks:
      - system-network
    ports:
      - "8088:8080"
    env_file: .env
    environment:
      - POSTGRES_DB=audit_db
    depends_on:
      discovery-service:
        condition: service_healthy
      config-service:
        condition: service_healthy
      db:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    healthcheck:
      test:
        ["CMD-SHELL", "curl -f http://localhost:8080/actuator/health || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 5
    develop:
      watch:
        - action: rebuild
          path: ./audit-service/src

  admin-service:
    build:
      context: ./admin-service
      dockerfile: Dockerfile
    image: rawad0/taxi-admin-service:latest
    container_name: taxi-admin-service
    networks:
      - system-network
    ports:
      - "8089:8080"
    env_file: .env
    environment:
      - POSTGRES_DB=admin_db
    depends_on:
      discovery-service:
        condition: service_healthy
      config-service:
        condition: service_healthy
      db:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    healthcheck:
      test:
        ["CMD-SHELL", "curl -f http://localhost:8080/actuator/health || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 5
    develop:
      watch:
        - action: rebuild
          path: ./admin-service/src

  bff-service:
    build:
      context: ./bff-service
      dockerfile: Dockerfile
    container_name: taxi-bff-service
    image: rawad0/taxi-bff-service:latest
    networks:
      - system-network
    ports:
      - "8090:8080"
    env_file: .env
    environment:
      - POSTGRES_DB=bff_db
    depends_on:
      discovery-service:
        condition: service_healthy
      config-service:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    healthcheck:
      test:
        ["CMD-SHELL", "curl -f http://localhost:8080/actuator/health || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 5
    develop:
      watch:
        - action: rebuild
          path: ./bff-service/src

  gateway-service:
    build:
      context: ./gateway-service
      dockerfile: Dockerfile
    image: rawad0/taxi-gateway-service:latest
    container_name: taxi-gateway-service
    networks:
      - system-network
    ports:
      - "8091:8080"
    env_file: .env
    depends_on:
      discovery-service:
        condition: service_healthy
      config-service:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test:
        ["CMD-SHELL", "curl -f http://localhost:8080/actuator/health || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 5
    develop:
      watch:
        - action: rebuild
          path: ./gateway-service/src

  monitoring-service:
    build:
      context: ./monitoring-service
      dockerfile: Dockerfile
    container_name: taxi-monitoring-service
    image: rawad0/taxi-monitoring-service:latest
    networks:
      - system-network
    ports:
      - "9091:8080"
    env_file: .env
    depends_on:
      discovery-service:
        condition: service_healthy
      config-service:
        condition: service_healthy
    healthcheck:
      test:
        ["CMD-SHELL", "curl -f http://localhost:8080/actuator/health || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 5
    develop:
      watch:
        - action: rebuild
          path: ./monitoring-service/src

networks:
  system-network:
    driver: bridge
